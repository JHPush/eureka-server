
# Eureka 서버 포트 설정
server:
  port: 8761

spring:
  application:
    name: eureka-server
  

# Eureka 서버가 자가 등록을 하지 않도록 설정한다.
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false